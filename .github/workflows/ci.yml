name: CI
on: [push, pull_request]
jobs:
  build-test:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install XcodeGen
        run: brew install xcodegen
      - name: Generate project
        run: xcodegen
      - name: Build
        run: xcodebuild -scheme Scratchpad -destination 'platform=macOS' MACOSX_DEPLOYMENT_TARGET=15.0 build
      - name: Test
        run: xcodebuild -scheme Scratchpad -destination 'platform=macOS' MACOSX_DEPLOYMENT_TARGET=15.0 test
